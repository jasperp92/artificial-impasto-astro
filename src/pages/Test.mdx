---
title: Test
layout: "@layouts/Layout.astro"
parent: ""
order: 7
---
import { Image } from 'astro:assets';
import Compare from "../components/Compare.astro";
import Layout from '../layouts/Layout.astro';
import beforeImg from "../assets/compare/schilf_1.jpg";
import afterImg  from "../assets/compare/schilf_2.jpg";
import Gallery from "@components/Gallery.astro";
import PageNav from "../components/PageNav.astro";
import ImageSequence from '@components/ImageSequence.astro';
import Lightbox from '@components/Lightbox.astro';
import Chart from "@components/Chart.svelte";

import data1 from "@data/data_1.json";
import data2 from "@data/data_2.json";


# Methode

<Compare client:load before={beforeImg} after={afterImg} maxWidth={1100} value={50} />

# h1
## h2
### h3
<b>bold Text</b><br />

<q class="block border-l-4 border-gray-400 pl-4 italic text-xl">
  Hier kommt ein Zitat rein
</q>
Hier kommt ganz normaler Text hin
<Gallery path="ip-adapter" />

<Compare before={beforeImg} after={afterImg} />

<ImageSequence folder="diffusion" mode="fade"/>




<div class="comment-right">Das Gefühl des Arbeitsprozesses ist bewusster, Bewussteres setzen von Pinsel und Stift, ich würde behaupten es gibt eine Art Aura die das Analoge hat und die beim digitalen oft verloren geht</div>

<div class="comment-left">Material übernimmt Teil der Arbeit :)</div>

![image](@images/comfyui_workflow.png)

<Lightbox />



<Reference 
  title="comfy.org" 
  authors="Yoland, Y., Huang, R., Dr.Lt.Data, comfyanonymous, & Byrne, C." 
  type="software"
  year="2025" 
  link="https://www.comfy.org/" 
  embed={true} />


import useofgenai from "@data/barchart_useofgenai.json";
import stacked from "@data/stacked-barchart.json";

### Falls du generative KI in deiner Gestaltung nutzt, warum?

<Chart 
client:load
  className="mx-auto"
  type="bar"
  data={useofgenai}
  options={{
    responsive: true,
    showTotal: false,
    indexAxis: "y"
  }}
/>

<Chart 
client:load
  className="mx-auto"
  type="bar"
  data={useofgenai}
  options={{
    responsive: true,
    showTotal: false,
    indexAxis: "x"
  }}
/>

<Chart
  client:load
  type="pie"
  data={data1}
  options={{ responsive: true }}
  className="max-w-150 mx-auto"
/>

<Chart 
client:load
  type="bar"
  data={stacked}
  options={{
    responsive: true,
    indexAxis: "y",
    scales: {
      x: {
        stacked: true,
      },
      y: {
        stacked: true
      }
      }
  }}
/>